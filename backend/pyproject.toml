[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "backend"
version = "0.1.0"
description = "Multi-tenant SaaS backend (FastAPI, PostgreSQL, Redis)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0,<0.129.0",
    "uvicorn[standard]>=0.32.0",
    "sqlalchemy>=2.0.0",
    "asyncpg",
    "redis>=5.0.0",
    "pydantic>=2.7.0,<3.0.0",
    "pydantic-settings>=2.0",
    "alembic",
    "python-dotenv",
    "pyjwt>=2.8.0",
    "passlib[bcrypt]>=1.7.4",
    "openai>=1.0.0",
    "stripe>=8.0.0",
    "arq>=0.26.0",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
    "httpx",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "integration: integration tests (DB, Redis)",
]

[tool.ruff]
line-length = 100
target-version = "py311"
